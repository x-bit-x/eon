{
  "name": "eon",
  "license": "AGPL-3.0",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "author": {
    "name": "Vitaliy Malyshev",
    "email": "zich.contemporary@mail.ru",
    "url": "https://github.com/x-bit-x"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/x-bit-x/eon"
  },
  "bugs": {
    "url": "https://github.com/x-bit-x/eon/issues"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "lint": "yarn lint:js && yarn lint:rust",
    "lint:js": "biome check src/ && tsc --noEmit",
    "lint:rust": "cd src-tauri && cargo clippy --all-targets --all-features -- -D warnings -D clippy::all && cargo fmt --all",
    "format": "yarn format:js && yarn format:rust",
    "format:rust": "cd src-tauri && cargo clippy --fix --allow-dirty --all-targets --all-features -- -D warnings -D clippy::all && cargo fmt --all",
    "format:js": "biome check src/ --write --unsafe"
  },
  "dependencies": {
    "@tauri-apps/plugin-opener": "^2",
    "@tauri-apps/api": "^2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.5",
    "@tauri-apps/cli": "^2",
    "@types/react": "^18.3.1",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "globals": "^16.4.0",
    "typescript": "~5.6.2",
    "vite": "^6.0.3"
  }
}
